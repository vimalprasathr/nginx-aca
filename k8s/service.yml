apiVersion: v1
kind: Service
metadata:
  name: argo-app-service
  namespace: argo-app
spec:
  selector:
    app: argo-app
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080



